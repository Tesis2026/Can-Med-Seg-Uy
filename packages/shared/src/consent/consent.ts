/**
 * Consentimiento informado (Ley 18.331) — notas de cliente en requirements/requirements.md.
 *
 * El texto vive en la web (`components/consent/consentCopy.ts`); acá solo la versión,
 * que la API persiste en `users.consent_version` para poder auditar qué texto aceptó
 * cada persona cuando el copy cambie.
 */
export const CONSENT_VERSION = "2026-05-16-ley-18331";
